debug: false
logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG

spring:
  ai:
    chat:
      client:
        observations:
          include-input: true
          include-output: true
          include-prompt: true

  data:
    mongodb:
      host: localhost
      port: 27017
      database: baeldung
      username: admin
      password: password
